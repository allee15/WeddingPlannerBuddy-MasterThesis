{
  "version": 2,
  "builds": [
    { "src": "api/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/weather", "dest": "/api/weather/predict.py" },
    { "src": "/api/(.*)", "dest": "api/$1/index.ts" }
  ]
}
